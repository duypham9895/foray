---
phase: 03-classifier-matcher
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/errors/index.ts
  - src/core/env.ts
  - src/features/classifier/rules.ts
  - src/features/classifier/rules.test.ts
  - src/features/classifier/thresholds.ts
  - src/features/classifier/thresholds.test.ts
  - src/features/classifier/budget.ts
  - src/features/classifier/budget.test.ts
  - .gitignore
autonomous: true
requirements: [CLASS-01, CLASS-03, CLASS-04]
must_haves:
  truths:
    - "Phase 3 has a typed error variant for budget exhaustion (errors.rateLimited reused, with a `budgetExhausted` factory if needed) and Anthropic API errors (errors.externalApi)"
    - "ANTHROPIC_API_KEY is required at runtime (no longer .optional()) and validated at module load by env.ts"
    - "rules.ts exports a typed CLASSIFICATION_RULES table that, for any given subject+excerpt, returns highest-priority match or 'unmatched' with documented confidence tier (0.95 explicit, 0.80 generic)"
    - "thresholds.ts exports THRESHOLDS as Record<EmailClassification, number> with EXACT values from CONTEXT §Area 3 and exports a meetsThreshold(label, confidence) predicate"
    - "budget.ts exports checkBudget() that reads data/classifier-log.jsonl, sums today's costUsd, returns err({_tag:'RateLimited', retryAfterSeconds}) at >= $0.50 or ok() otherwise; FAILS CLOSED on file-read error"
    - "budget.ts exports appendCostEntry({inputTokens, outputTokens, model, emailHash}) that appends one JSON line with computed costUsd using literal Anthropic Haiku 4.5 prices ($0.80/MTok input, $4.00/MTok output)"
    - "Tests: rules.ts ≥30 unit tests covering label match paths, priority order, tier confidence; thresholds.ts ≥6 unit tests; budget.ts ≥5 unit tests using an in-test temp directory for the JSONL log"
    - "Pre-commit gate (lint, typecheck, test:run, build, depcheck) green"
  artifacts:
    - path: "src/features/classifier/rules.ts"
      provides: "Typed regex tier table + classifyByRules({subject, bodyExcerpt}) pure function"
      contains: "export const CLASSIFICATION_RULES"
    - path: "src/features/classifier/thresholds.ts"
      provides: "Per-label asymmetric threshold map + meetsThreshold predicate"
      contains: "export const THRESHOLDS"
    - path: "src/features/classifier/budget.ts"
      provides: "checkBudget() pre-call guard + appendCostEntry() post-call recorder"
      contains: "export async function checkBudget"
    - path: "src/core/errors/index.ts"
      provides: "errors.rateLimited reused for budget; errors.externalApi reused for Anthropic; verify both are present"
      contains: "rateLimited"
    - path: "src/core/env.ts"
      provides: "ANTHROPIC_API_KEY required (no longer optional)"
      contains: "ANTHROPIC_API_KEY"
    - path: ".gitignore"
      provides: "data/ already gitignored — verify"
      contains: "/data/"
  key_links:
    - from: "src/features/classifier/budget.ts"
      to: "data/classifier-log.jsonl"
      via: "fs.readFile + line-by-line JSON.parse"
      pattern: "classifier-log\\.jsonl"
    - from: "src/features/classifier/thresholds.ts"
      to: "@/generated/prisma/client EmailClassification enum"
      via: "type import for the THRESHOLDS Record key"
      pattern: "EmailClassification"
    - from: "src/features/classifier/rules.ts"
      to: "@/generated/prisma/client EmailClassification enum"
      via: "label values match the enum"
      pattern: "EmailClassification"
---

<objective>
Build the four pure utilities that the classifier service (Plan 03-02) composes:
(1) error taxonomy is verified to support `RateLimited` (budget) and `ExternalApi` (Anthropic);
(2) `ANTHROPIC_API_KEY` is required at runtime;
(3) `rules.ts` — typed tier table + `classifyByRules()` pure function;
(4) `thresholds.ts` — per-label asymmetric threshold map + `meetsThreshold()` predicate;
(5) `budget.ts` — pre-call `checkBudget()` guard + `appendCostEntry()` recorder, both reading/writing `data/classifier-log.jsonl` with literal Haiku 4.5 prices.

These are self-contained and have NO Anthropic SDK dependency at runtime — Plan 03-02 wires that. Splitting them out lets us test the cost arithmetic, the regex priorities, and the threshold table in isolation, where the LLM is not mocked because it's not even imported.

Purpose: every trust-trio knob the classifier needs (rules-first matching, asymmetric thresholds, budget gate) lands as testable pure code BEFORE the SDK call wraps them. This is the deliberate inversion of "implement the LLM call first, bolt on guards later" — guards are the contract; the LLM call honors them.

Output:
- src/features/classifier/{rules,thresholds,budget}.ts + colocated .test.ts files
- src/core/errors/index.ts verified present (no edit if rateLimited + externalApi already exist)
- src/core/env.ts: ANTHROPIC_API_KEY moves from optional → required
- .gitignore: confirm /data/ + classifier-log.jsonl already covered
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-classifier-matcher/03-CONTEXT.md
@PRINCIPLES.md
@CLAUDE.md
@AGENTS.md
@.planning/research/PITFALLS.md

@src/core/errors/index.ts
@src/core/env.ts
@.gitignore
@prisma/schema.prisma

<interfaces>
<!-- Existing contracts the executor will use directly. No codebase exploration needed. -->

From src/core/errors/index.ts (already exists):
```typescript
export type AppError =
  | { _tag: 'NotFound';     resource: string; id: string }
  | { _tag: 'Unauthorized' }
  | { _tag: 'Forbidden';    reason: string }
  | { _tag: 'Validation';   issues: ZodIssue[] }
  | { _tag: 'ExternalApi';  service: 'gmail' | 'llm'; cause: unknown }
  | { _tag: 'Db';           cause: unknown }
  | { _tag: 'RateLimited';  retryAfterSeconds: number }
  | { _tag: 'Conflict';     reason: string }

export const errors = {
  // ...
  externalApi:  (service: 'gmail' | 'llm', cause: unknown): AppError => ({ _tag: 'ExternalApi', service, cause }),
  rateLimited:  (retryAfterSeconds: number): AppError => ({ _tag: 'RateLimited', retryAfterSeconds }),
}
export { ok, err, Result } from 'neverthrow'
```

Both `RateLimited` and `ExternalApi` (`service: 'llm'`) ALREADY EXIST. Task 1 below VERIFIES them; do NOT add a new variant.

From prisma/schema.prisma (already exists):
```prisma
enum EmailClassification {
  rejection
  interview_invite
  recruiter_outreach
  noise
  unmatched
}
```

Import as: `import type { EmailClassification } from '@/generated/prisma/client'`

From src/core/env.ts (existing — needs change):
```typescript
ANTHROPIC_API_KEY: z.string().optional(),  // ← becomes required (Task 1)
CLASSIFIER_AUTO_THRESHOLD: z.coerce.number().min(0).max(1).default(0.85),
  // ← REJECTED by CONTEXT §Area 3 ("a single CLASSIFIER_AUTO_THRESHOLD env var
  // is NOT the gate"). Plan 03-01 KEEPS this env var for now (Phase 4 may use
  // it as a floor; v1 deferred entries call it out). Do NOT remove. Per-label
  // gates live in code (thresholds.ts).
```

From CONTEXT §Area 3 (LOCKED — exact values):
```
rejection: 0.92
interview_invite: 0.85
recruiter_outreach: 0.80
noise: 0.70
unmatched: 1.0  // unmatched can never auto-act
```

From CONTEXT §Area 4 (LOCKED):
- Daily cap: $0.50 USD
- Anthropic Haiku 4.5 pricing (as of 2026-01): $0.80 per million input tokens, $4.00 per million output tokens
- Cost log line shape: `{"ts":"ISO","model":"claude-haiku-4-5","inputTokens":N,"outputTokens":N,"costUsd":N,"emailHash":"sha256:..."}`
- emailHash = sha256 of normalized `subject + '\n' + bodyExcerpt` (privacy-preserving identifier)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify error taxonomy + require ANTHROPIC_API_KEY</name>
  <files>src/core/errors/index.ts, src/core/env.ts, src/core/errors/index.test.ts (verify only — file may not exist)</files>
  <read_first>src/core/errors/index.ts (full); src/core/env.ts (full); .env.example (full)</read_first>
  <action>
**Step A — Verify errors taxonomy (no code change expected).**
1. Read `src/core/errors/index.ts`. Verify the `AppError` union contains `{ _tag: 'RateLimited'; retryAfterSeconds: number }` AND `{ _tag: 'ExternalApi'; service: 'gmail' | 'llm'; cause: unknown }`. Verify the `errors` factory exports `rateLimited(retryAfterSeconds)` and `externalApi(service, cause)`. If both are present, do NOT modify the file.
2. If either is missing (you should NOT find this — the file already has both), add the missing factory using the existing pattern; do NOT introduce a new variant name.

**Step B — Promote ANTHROPIC_API_KEY to required.**
1. In `src/core/env.ts`, change `ANTHROPIC_API_KEY: z.string().optional()` to `ANTHROPIC_API_KEY: z.string().min(1, 'ANTHROPIC_API_KEY is required for classifier (CLASS-02). Get one from https://console.anthropic.com/settings/keys')`. Do NOT touch any other env var.
2. Update `.env.example` (read first; if `ANTHROPIC_API_KEY` is already listed, leave the entry alone but update the comment to say "REQUIRED (Phase 3 classifier)"; if absent, add the line `ANTHROPIC_API_KEY=  # REQUIRED (Phase 3 classifier — get one from https://console.anthropic.com/settings/keys)`).
3. Do NOT remove `CLASSIFIER_AUTO_THRESHOLD` — it stays in the schema for backward-compat; per-label thresholds live in code (Task 3).

**Step C — Verify .gitignore covers the cost log.**
1. Read `.gitignore`. Verify both `/data/` and `classifier-log.jsonl` lines are present (they are — confirm only). No edit if both present.

**Pre-flight requirement for executor:** `ANTHROPIC_API_KEY` must be set in the local `.env` BEFORE running `pnpm build` after this task. If absent, build will fail at env validation. Document this in the SUMMARY.

Per Karpathy §1.3 (surgical), only the three lines above change. Per CLAUDE.md §6, no key value is logged.
  </action>
  <verify>
    <automated>cd /Users/edwardpham/Documents/Programming/Projects/foray && grep -c "_tag: 'RateLimited'" src/core/errors/index.ts && grep -c "_tag: 'ExternalApi'" src/core/errors/index.ts && grep -c "ANTHROPIC_API_KEY: z.string().min(1" src/core/env.ts && grep -c "/data/" .gitignore && grep -c "classifier-log.jsonl" .gitignore && pnpm typecheck</automated>
  </verify>
  <done>RateLimited + ExternalApi present in errors.ts; ANTHROPIC_API_KEY requires non-empty string in env.ts; /data/ and classifier-log.jsonl in .gitignore; typecheck clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rules table + classifyByRules() + thresholds + meetsThreshold()</name>
  <files>src/features/classifier/rules.ts, src/features/classifier/rules.test.ts, src/features/classifier/thresholds.ts, src/features/classifier/thresholds.test.ts</files>
  <behavior>
**rules.ts (CLASS-01):**
- exports `CLASSIFICATION_RULES`: typed `ReadonlyArray<{label: EmailClassification, pattern: RegExp, confidence: 0.95 | 0.80, source: 'subject' | 'body' | 'either'}>`
- exports `classifyByRules(input: {subject: string, bodyExcerpt: string}): {label: EmailClassification, confidence: number, classifiedBy: 'rules', matchedRuleIndex?: number}` — pure, no I/O, no DB
- Match strategy (LOCKED in CONTEXT §Area 1):
  - Scan subject FIRST then body for each rule entry
  - First match wins per label
  - Multiple labels matching → return highest-priority: `rejection > interview_invite > recruiter_outreach > noise > unmatched`
  - No rule matched → `{label: 'unmatched', confidence: 0, classifiedBy: 'rules'}`
- Starting regex set (refine if fixture tests in Plan 03-04 fail):
  - `rejection` (0.95): `/we (have decided|regret to inform).{0,40}(not move forward|not be moving forward|other (candidates|applicants))/i`
  - `rejection` (0.80): `/(thank you for your interest|after careful consideration)/i`
  - `interview_invite` (0.95): `/(would (you )?like to (set up|schedule)|propose times for|next step.{0,30}(call|interview))/i`
  - `interview_invite` (0.80): `/(scheduling|book a time|calendar invite)/i`
  - `recruiter_outreach` (0.95): `/(saw your (profile|background)|came across your (resume|profile)|reach out about a role)/i`
  - `recruiter_outreach` (0.80): `/(opportunity|opening|position) (at|with) /i`
  - `noise` (0.95): `/(unsubscribe|view in browser|update your preferences|manage your subscriptions)/i`

**Test cases for rules.test.ts (≥30 tests):**
- Test 1-7: each label has at least one positive 0.95-tier match (subject-side)
- Test 8-14: each label has at least one positive 0.80-tier match
- Test 15-19: body-side matching works (subject empty, body has match)
- Test 20-22: priority — when both `rejection` and `interview_invite` match, returns rejection
- Test 23: when both `noise` and `recruiter_outreach` match, returns recruiter_outreach (higher priority)
- Test 24: empty subject + empty body → unmatched
- Test 25: no-match returns confidence: 0 with classifiedBy: 'rules'
- Test 26: case-insensitivity — uppercase subject still matches
- Test 27: subject scanned first — if subject has 0.95 rejection AND body has 0.95 interview_invite, rejection wins (priority)
- Test 28: subject 0.80 vs body 0.95 of SAME label — returns first match, which is the 0.80 subject (per "first match wins per label" rule)
- Test 29: noise pattern doesn't fire on a real interview_invite (negative test — no false positive)
- Test 30: recruiter_outreach 0.95 pattern fires on "I came across your profile and wanted to reach out about a role"
- Test 31+: at planner discretion — add coverage for any rule that feels under-tested

**thresholds.ts (CLASS-03):**
- exports `THRESHOLDS: Record<EmailClassification, number>` with the EXACT values:
  ```ts
  export const THRESHOLDS: Record<EmailClassification, number> = {
    rejection: 0.92,
    interview_invite: 0.85,
    recruiter_outreach: 0.80,
    noise: 0.70,
    unmatched: 1.0,
  } as const
  ```
- exports `meetsThreshold(label: EmailClassification, confidence: number): boolean` — returns `confidence >= THRESHOLDS[label]`. Phase 4's act-stage uses this; Phase 3 ships it.
- Module-load-time invariant: `THRESHOLDS.rejection > THRESHOLDS.interview_invite` (asymmetric per CONTEXT §Area 3 — Pitfall #4). Encode as a TypeScript-level comment + a runtime test, NOT an assertion (we don't want module-load throws in production).

**Test cases for thresholds.test.ts (≥6 tests):**
- T1: THRESHOLDS has exactly 5 keys matching the EmailClassification enum (no missing, no extra)
- T2: rejection (0.92) > interview_invite (0.85) — asymmetry invariant
- T3: meetsThreshold('rejection', 0.92) === true (boundary inclusive)
- T4: meetsThreshold('rejection', 0.91) === false
- T5: meetsThreshold('unmatched', 1.0) === true; meetsThreshold('unmatched', 0.99) === false (unmatched never auto-acts at <1.0)
- T6: meetsThreshold('noise', 0.70) === true; meetsThreshold('noise', 0.69) === false
  </behavior>
  <read_first>src/features/applications/schema.ts (for the eventDataSchemas pattern); prisma/schema.prisma (for the EmailClassification enum); src/features/classifier/ (verify directory does not exist or is empty)</read_first>
  <action>
1. Create `src/features/classifier/rules.ts` per the behavior block. Import `EmailClassification` as `import type { EmailClassification } from '@/generated/prisma/client'`. The file ends with the `classifyByRules` export. Add a top-of-file JSDoc explaining: tier semantics (0.95 / 0.80), priority order, and the "scan subject first then body, first match wins per label" rule. NO Anthropic import.
2. Create `src/features/classifier/rules.test.ts` per the behavior block. Use `import { describe, it, expect } from 'vitest'`. ≥30 tests, each with `describe`+`it` describing the behavior verified. Use real-shaped subject/body strings (not "test test test" filler).
3. Create `src/features/classifier/thresholds.ts` per the behavior block. Pure module — only the `THRESHOLDS` const, the `meetsThreshold` function, and a top-of-file JSDoc citing CONTEXT §Area 3 + ADR-0012 amendment (Plan 03-05 will write the amendment).
4. Create `src/features/classifier/thresholds.test.ts` per the behavior block. ≥6 tests.
5. Run `pnpm test:run -- rules` and `pnpm test:run -- thresholds` separately during dev to verify each passes; final pre-commit gate runs the full suite.

Per Karpathy §1.2 (simplicity) — ZERO speculative exports. No "future-proofing" enums. No abstract `Rule` class. The shape is `{label, pattern, confidence, source}` and that is the ENTIRE shape. The `classifyByRules` function is one switch / for-loop, no scoring engine.

Per AGENTS.md "no `prisma` imports outside `src/core/db/`": this file imports `EmailClassification` from `@/generated/prisma/client` which IS allowed (it's a TYPE import, not a runtime Prisma call). Use `import type` (verbatimModuleSyntax requires it).
  </action>
  <verify>
    <automated>cd /Users/edwardpham/Documents/Programming/Projects/foray && pnpm test:run src/features/classifier/rules.test.ts src/features/classifier/thresholds.test.ts && pnpm typecheck && grep -c "rejection: 0.92" src/features/classifier/thresholds.ts && grep -c "interview_invite: 0.85" src/features/classifier/thresholds.ts && grep -c "noise: 0.70" src/features/classifier/thresholds.ts && grep -c "unmatched: 1.0\|unmatched: 1$" src/features/classifier/thresholds.ts && grep -c "import type { EmailClassification }" src/features/classifier/rules.ts</automated>
  </verify>
  <done>rules.test.ts has ≥30 passing tests; thresholds.test.ts has ≥6 passing tests; THRESHOLDS contains all 5 exact values; classifyByRules() is exported; typecheck clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Budget guard + cost recorder (data/classifier-log.jsonl)</name>
  <files>src/features/classifier/budget.ts, src/features/classifier/budget.test.ts</files>
  <behavior>
**budget.ts (CLASS-04):**
- exports `DAILY_BUDGET_USD = 0.50` constant (LOCKED in CONTEXT §Area 4)
- exports Anthropic Haiku 4.5 pricing constants (LOCKED, comment with source/date):
  ```ts
  // Anthropic Claude Haiku 4.5 pricing (as of 2026-01-15)
  // Source: https://www.anthropic.com/pricing
  export const HAIKU_INPUT_USD_PER_MTOK = 0.80   // $0.80 per million input tokens
  export const HAIKU_OUTPUT_USD_PER_MTOK = 4.00  // $4.00 per million output tokens
  ```
- exports `LOG_PATH` = `path.join(process.cwd(), 'data', 'classifier-log.jsonl')` — overridable via `process.env.CLASSIFIER_LOG_PATH` (test seam ONLY; not documented in .env.example because not user-configurable in production)
- exports `computeCostUsd(inputTokens, outputTokens): number` — pure: `(inputTokens / 1e6) * HAIKU_INPUT_USD_PER_MTOK + (outputTokens / 1e6) * HAIKU_OUTPUT_USD_PER_MTOK`. Returns full precision; do NOT round (rounding is presentation; aggregation is math).
- exports `async function checkBudget(): Promise<Result<void, AppError>>` — reads `LOG_PATH` line-by-line, parses each JSON, sums `costUsd` for entries with `ts` starting with today's `YYYY-MM-DD`. If sum >= DAILY_BUDGET_USD → `err(errors.rateLimited(secondsUntilMidnight()))`. If file does not exist → `ok()` (no log = no cost). If file exists but is unreadable / contains invalid JSON → **FAIL CLOSED**: `err(errors.rateLimited(60))` and log a `pino.error` with `{op: 'classifier.budget.read', cause}`. (Per CONTEXT §security: failed budget read should not allow LLM call.)
- exports `async function appendCostEntry(entry: {inputTokens, outputTokens, model, emailHash}): Promise<Result<{costUsd: number}, AppError>>` — computes `costUsd` via `computeCostUsd`, builds a JSONL row `{ts: new Date().toISOString(), model, inputTokens, outputTokens, costUsd, emailHash}`, ensures `data/` directory exists (`fs.mkdir({recursive: true})`), appends one line + `\n`. On any I/O failure → `err(errors.db(cause))` (we have no `WriteFailed` variant; `Db` is appropriate for "local persistent storage failed").
- exports helper `secondsUntilMidnight(now = new Date()): number` (pure, exported for test).
- exports helper `hashEmailContent(subject, bodyExcerpt): string` — returns `'sha256:' + crypto.createHash('sha256').update(subject + '\n' + bodyExcerpt).digest('hex')`. Used by Plan 03-02 service to populate `emailHash`. PURE — no I/O.

**Test cases for budget.test.ts (≥5 tests, plus discretion):**
- Use a per-test temp directory (`fs.mkdtemp(os.tmpdir() + '/foray-budget-')`) and set `process.env.CLASSIFIER_LOG_PATH` for the duration of the test (restore in `afterEach`). DO NOT touch the real `data/classifier-log.jsonl`.
- T1: empty (non-existent) log → checkBudget returns ok()
- T2: today's entries summing to $0.49 → checkBudget returns ok()
- T3: today's entries summing to $0.50 → checkBudget returns err({_tag:'RateLimited'}) AND retryAfterSeconds is between 0 and 86400
- T4: today's entries summing to $1.00 (over) → checkBudget returns err({_tag:'RateLimited'})
- T5: yesterday's entries summing to $1.00 + today's entries summing to $0.10 → ok() (only today counts)
- T6: file exists but contains a malformed line → FAIL CLOSED: err({_tag:'RateLimited'}, retryAfterSeconds: 60)
- T7: appendCostEntry creates data/ if missing AND appends one valid JSON line (parse it back, verify shape: ts, model, inputTokens, outputTokens, costUsd, emailHash)
- T8: computeCostUsd(1_000_000, 0) === 0.80; computeCostUsd(0, 1_000_000) === 4.00; computeCostUsd(1_000_000, 1_000_000) === 4.80
- T9: hashEmailContent is deterministic — same input → same hash; different input → different hash
- T10: hashEmailContent always returns 'sha256:' + 64 hex chars
  </behavior>
  <read_first>src/features/classifier/ (after Task 2 — should now contain rules.ts + thresholds.ts)</read_first>
  <action>
1. Create `src/features/classifier/budget.ts` per the behavior block.
   - Use `import 'server-only'` at the top (this file does I/O).
   - Use `import { promises as fs } from 'node:fs'`, `import path from 'node:path'`, `import crypto from 'node:crypto'`.
   - Import `Result, ok, err, errors, AppError` from `@/core/errors`.
   - Use `pino` from `@/core/logger` if it exists; otherwise use `console.error` for the FAIL-CLOSED path with a comment "TODO(duy, 2026-05-09): swap to pino logger when @/core/logger lands". (Logger may not exist yet — check first.)
   - Wrap fs reads/writes via `fromPromise` (neverthrow) OR direct try/catch returning Result; either is acceptable per PRINCIPLES.md §"Where throw is legal" — keep consistent.
2. Create `src/features/classifier/budget.test.ts` per the behavior block. Use `import { describe, it, expect, beforeEach, afterEach } from 'vitest'`. Use `os.tmpdir()` for the temp log path. RESTORE `process.env.CLASSIFIER_LOG_PATH` in afterEach.
3. The cost log file `data/classifier-log.jsonl` is NEVER created by tests against the real path. Only the temp file is touched.
4. Run `pnpm test:run src/features/classifier/budget.test.ts` to verify isolation.

Per Karpathy §1.2: NO "log rotation", NO "tiered budget warning at 80%", NO "configurable budget per label". The CONTEXT explicitly defers rotation to Standard milestone. The 50% warning is monitoring; we have control.

Per CLAUDE.md §6 (privacy): the `emailHash` is a SHA256 of subject+body content — privacy-preserving. NEVER log raw subject or body in this file. The hash is the audit identifier.

Per the threat model below: failed budget read returns RateLimited (not RateLimited 0 or ok), so the orchestrator in Plan 03-02 cannot accidentally proceed.
  </action>
  <verify>
    <automated>cd /Users/edwardpham/Documents/Programming/Projects/foray && pnpm test:run src/features/classifier/budget.test.ts && pnpm typecheck && grep -c "DAILY_BUDGET_USD = 0.50\|DAILY_BUDGET_USD = 0\.5" src/features/classifier/budget.ts && grep -c "HAIKU_INPUT_USD_PER_MTOK = 0.80\|HAIKU_INPUT_USD_PER_MTOK = 0\.8" src/features/classifier/budget.ts && grep -c "HAIKU_OUTPUT_USD_PER_MTOK = 4.00\|HAIKU_OUTPUT_USD_PER_MTOK = 4$" src/features/classifier/budget.ts && grep -c "checkBudget" src/features/classifier/budget.ts && grep -c "appendCostEntry" src/features/classifier/budget.ts && grep -c "hashEmailContent" src/features/classifier/budget.ts</automated>
  </verify>
  <done>budget.test.ts ≥5 tests passing using temp log path; checkBudget exported; appendCostEntry exported; hashEmailContent exported; pricing constants exact; typecheck clean. Pre-commit gate (lint + typecheck + test:run + build + depcheck) MUST be re-run after Task 3 and pass before commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process.env → env.ts | ANTHROPIC_API_KEY is parsed at module load; throw on missing |
| local FS → budget.ts | data/classifier-log.jsonl is read on every checkBudget call; trust file integrity (FAIL CLOSED on parse failure) |
| caller → classifyByRules | subject + bodyExcerpt are untrusted strings; rules.ts uses regex with bounded `.{0,40}` quantifiers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Information Disclosure | budget.ts cost log | mitigate | Log entries store `emailHash: sha256:...` not raw email content (CLAUDE.md §6). Test T9-T10 verify hash format. |
| T-03-01-02 | Denial of Service | budget.ts read failure | mitigate | FAIL CLOSED — file-read or parse error returns err(rateLimited(60)). Test T6 verifies. Without this, a corrupted log file would silently allow unbounded LLM spend. |
| T-03-01-03 | Tampering | budget.ts cost log | accept | Local-first single-user; user can edit data/classifier-log.jsonl to bypass the cap. Documented as acceptable risk for Lean (the cap exists to catch BUGS, not to defend against the user). |
| T-03-01-04 | Information Disclosure | env.ts ANTHROPIC_API_KEY | mitigate | Pino redaction list already includes `*.authorization` + `password`. Add explicit `'env.ANTHROPIC_API_KEY'` redaction path when @/core/logger module is added (out of scope this plan; flag as TODO). NEVER log the key directly. |
| T-03-01-05 | Denial of Service | rules.ts regex | mitigate | All regexes use bounded quantifiers (`.{0,40}` not `.+`). No catastrophic-backtracking shapes. Reviewer must inspect each new regex for ReDoS-prone patterns (e.g., `(a+)+`). |
| T-03-01-06 | Repudiation | budget.ts cost log | accept | Cost log is single-source-of-truth for budget; no second copy. Acceptable for single-user Lean. Standard milestone: rotate daily + sign entries. |
| T-03-01-07 | Spoofing | classifyByRules() input | accept | Function trusts its caller (Plan 03-02 service) to provide the parsed subject/excerpt. No auth surface here. |
</threat_model>

<verification>
After all 3 tasks:
1. `cd /Users/edwardpham/Documents/Programming/Projects/foray && pnpm lint && pnpm typecheck && pnpm test:run && pnpm build && pnpm depcheck` — all green. Pre-commit gate (CLAUDE.md §2.1) is non-negotiable; do NOT use `--no-verify`. If `pnpm build` fails because ANTHROPIC_API_KEY is missing from local `.env`, that confirms Task 1 worked — add the key to local `.env` and retry.
2. `grep -c "_tag: 'RateLimited'\|_tag: 'ExternalApi'" src/core/errors/index.ts` ≥ 2.
3. Total new/modified files exactly: 6 created (rules.ts, rules.test.ts, thresholds.ts, thresholds.test.ts, budget.ts, budget.test.ts) + 2 modified (env.ts, .env.example). errors/index.ts and .gitignore are READ-ONLY verifications (no edits if invariants already hold).
4. New tests added: ≥41 (30 rules + 6 thresholds + 5 budget). Total test count after: previous count + ≥41.
5. No Anthropic SDK import anywhere in this plan's files. Plan 03-02 owns that.
6. No direct prisma access. `EmailClassification` is a TYPE import only.
</verification>

<success_criteria>
- All three pure utility modules ship with green tests
- Pre-commit gate green
- Phase 3's typed config (THRESHOLDS) has the EXACT values from CONTEXT §Area 3, ready for Phase 4 to import via `THRESHOLDS[label]`
- Budget guard FAILS CLOSED on read failure — Plan 03-02's classifyEmail will not bypass it
- Cost arithmetic is unit-tested with exact published Haiku 4.5 prices, with the source/date in a code comment for future updates
- Phase 4 has the THRESHOLDS map + meetsThreshold() ready to import without re-deriving the rule
</success_criteria>

<output>
After completion, create `.planning/phases/03-classifier-matcher/03-01-SUMMARY.md` per the template. Include:
- Function signatures for `classifyByRules`, `meetsThreshold`, `checkBudget`, `appendCostEntry`, `hashEmailContent`
- The exact THRESHOLDS object (5 entries)
- The pricing constants (3 numbers)
- Self-check: 6 created + 2 modified files exist; tests pass count
- Phase 4 readiness: list of exports ready to import
</output>
