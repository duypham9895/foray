---
phase: 03-classifier-matcher
plan: 05
type: execute
wave: 4
depends_on: [03-01, 03-02, 03-03, 03-04]
files_modified:
  - docs/decisions/0012-status-regression-block-and-auto-update-styling.md
  - .planning/phases/03-classifier-matcher/03-UAT.md
autonomous: true
requirements: [CLASS-03]
must_haves:
  truths:
    - "ADR-0012 is amended (in-place — not superseded; the existing ADR is not yet shipped to production) with a new ## section 'Asymmetric per-label thresholds' that documents the 0.92 / 0.85 / 0.80 / 0.70 / 1.0 values + rationale (Pitfall #4) + the typed const location (src/features/classifier/thresholds.ts) + the meetsThreshold predicate"
    - "ADR-0012 references the implementation file (thresholds.ts) at least 3 times so a future redesign can find both via git grep"
    - "ADR-0012 explicitly states the rejection path: a single env-var blanket gate (CLASSIFIER_AUTO_THRESHOLD) is REJECTED — the env var stays in env.ts as a deprecated tunable but is NOT consumed by the act-stage gate"
    - "03-UAT.md is the structured pre-merge UAT artifact (mirrors 02-UAT.md pattern). Records the 5 ROADMAP success criteria + 7 requirement IDs + ≥4 manual-verification items (e.g., 'set ANTHROPIC_API_KEY in .env', 'check data/classifier-log.jsonl exists after first LLM call'). Each row has a status: PASS / FAIL / DEFERRED. Total: ≥10 PASS|FAIL|DEFERRED tokens."
    - "Final pre-commit gate (lint + typecheck + test:run + build + depcheck) re-run as the LAST action — all green"
    - "STATE.md, ROADMAP.md, REQUIREMENTS.md NOT touched — those are the orchestrator's responsibility"
  artifacts:
    - path: "docs/decisions/0012-status-regression-block-and-auto-update-styling.md"
      provides: "Amended ADR — adds 'Asymmetric per-label thresholds' section"
      contains: "Asymmetric per-label thresholds"
    - path: ".planning/phases/03-classifier-matcher/03-UAT.md"
      provides: "Phase 3 pre-merge UAT artifact"
      contains: "PASS\\|FAIL\\|DEFERRED"
  key_links:
    - from: "docs/decisions/0012-status-regression-block-and-auto-update-styling.md"
      to: "src/features/classifier/thresholds.ts"
      via: "literal reference in ADR body"
      pattern: "thresholds\\.ts"
    - from: ".planning/phases/03-classifier-matcher/03-UAT.md"
      to: ".planning/phases/03-classifier-matcher/03-CONTEXT.md"
      via: "criterion ↔ requirement ID mapping"
      pattern: "CLASS-\\|MATCH-"
---

<objective>
Phase 3 close-out — two documentation artifacts that lock the classifier+matcher contracts before Phase 4 wires them into the live pipeline:

1. **ADR-0012 amendment** — adds the "Asymmetric per-label thresholds" section to the existing ADR (which already covers status-regression block + auto-update visual treatment from Phase 2). Phase 3 contributes the third trust-trio leg: the per-label threshold map. Phase 4's act-stage will read this section before importing `THRESHOLDS` and `meetsThreshold` from `src/features/classifier/thresholds.ts`.

2. **03-UAT.md** — structured pre-merge UAT artifact mirroring Phase 2's 02-UAT.md pattern. Records every ROADMAP success criterion + every CLASS-XX/MATCH-XX requirement ID + the manual verification items (set ANTHROPIC_API_KEY, run a real LLM call once, confirm budget log file populates). Most rows are DEFERRED to the owner's pre-merge browser/CLI walkthrough; the in-code criteria are PASS now.

Plus: re-run the final pre-commit gate one last time across the entire Phase 3 surface.

Purpose: when Phase 4's `inbox/act` orchestrator is being built, Plan 04-XX cannot accidentally re-derive the threshold values, re-implement the budget gate, or skip the asymmetric thresholds in favor of the env var. ADR-0012 is the authoritative source. Plan 03-05 makes this contract grep-discoverable and immutable (without supersession).

Output:
- docs/decisions/0012-status-regression-block-and-auto-update-styling.md (amended in place)
- .planning/phases/03-classifier-matcher/03-UAT.md (new)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-classifier-matcher/03-CONTEXT.md
@.planning/phases/03-classifier-matcher/03-01-SUMMARY.md
@.planning/phases/03-classifier-matcher/03-02-SUMMARY.md
@.planning/phases/03-classifier-matcher/03-03-SUMMARY.md
@.planning/phases/03-classifier-matcher/03-04-SUMMARY.md
@docs/decisions/0012-status-regression-block-and-auto-update-styling.md
@.planning/phases/02-applications-slice-manual-tracker/02-UAT.md
@PRINCIPLES.md
@CLAUDE.md
@.planning/research/PITFALLS.md

@src/features/classifier/thresholds.ts
@src/features/classifier/budget.ts
@src/features/classifier/service.ts
@src/features/matcher/service.ts

<existing_patterns>
- ADR-0011 (`docs/decisions/0011-rls-via-with-rls-helper.md`) and ADR-0012 (referenced) establish the "do NOT silently edit; supersede via new ADR" convention. Plan 03-05 deliberately AMENDS ADR-0012 (not supersedes) because:
  - The existing ADR-0012 was written in Phase 2 (last commit ~2 days ago in the same milestone)
  - The original ADR's intent INCLUDED the trust trio per ROADMAP.md "Cross-Cutting Concerns"
  - Phase 3's threshold section is the natural completion of the trio that Phase 2 deliberately left for Phase 3
  - Amendment in this case is the smallest correct thing per Karpathy §1.2
- The amendment adds ONE new ## section. It does NOT edit existing sections (Context, Decision A, Decision B, Consequences, etc.). The new section is appended BEFORE "## When we'd reconsider" to keep the document's logical flow.
- 02-UAT.md format: structured table with ID, Description, Status columns. ≥10 PASS|FAIL|DEFERRED tokens. Covers ROADMAP success criteria + Lean acceptance items + plan-task verification items.
</existing_patterns>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend ADR-0012 with asymmetric thresholds section</name>
  <files>docs/decisions/0012-status-regression-block-and-auto-update-styling.md</files>
  <read_first>docs/decisions/0012-status-regression-block-and-auto-update-styling.md (full); src/features/classifier/thresholds.ts (Plan 03-01 output); .planning/research/PITFALLS.md §"Pitfall 4"; CONTEXT §Area 3</read_first>
  <action>
1. Read the current ADR-0012. Verify the existing structure: Context, Decision A (status regression), Decision B (visual treatment), Consequences, When we'd reconsider, References, Supersedes.

2. Insert a NEW Decision section labeled `### C. Asymmetric per-label classifier thresholds (Phase 3 — added 2026-05-09)` between the existing `### B. Auto-update Event renders ...` section and `## Consequences`. Also add a corresponding sub-context `### Sub-context C — per-label threshold asymmetry` under the existing Context section.

3. Sub-context C content (before ## Decision):
   ```markdown
   ### Sub-context C — per-label threshold asymmetry

   PITFALLS.md §4 documents the central trust risk: the FIRST wrong auto-classification
   destroys trust before undo can save it. The asymmetric cost is the key insight —
   a false-positive `rejection` on an active interview is *catastrophic* (the owner
   may stop replying to the recruiter), while a false-negative (item lands in
   review queue) is *zero cost*.

   The original `env.CLASSIFIER_AUTO_THRESHOLD` (single number, default 0.85)
   encodes the wrong assumption that all labels carry equal cost. A 0.85 threshold
   is too lax for `rejection` (which needs to be near-certain to auto-act) and too
   strict for `noise` (where filtering aggressively is the goal — false positives
   just sit in review queue harmlessly).

   Phase 3's classifier slice introduces a typed map of asymmetric thresholds
   that supersedes the single env-var gate.
   ```

4. Decision C content (between Decision B and ## Consequences):
   ```markdown
   ### C. Asymmetric per-label classifier thresholds (Phase 3 — added 2026-05-09)

   The single source of truth for the auto-act gate is
   `src/features/classifier/thresholds.ts`, which exports a typed
   `THRESHOLDS: Record<EmailClassification, number>` and a `meetsThreshold(label, confidence): boolean`
   predicate. Phase 4's `inbox/act` stage MUST use `meetsThreshold(cls.label, cls.confidence)`
   as the auto-act gate — NOT `cls.confidence >= env.CLASSIFIER_AUTO_THRESHOLD`.

   Locked threshold values (Phase 3 CONTEXT §Area 3):

   | Label              | Threshold | Rationale |
   |--------------------|-----------|-----------|
   | `rejection`        | **0.92**  | HIGH bar — false positive is catastrophic (PITFALLS.md §4). Better to land in review than to silently un-engage with a live opportunity. |
   | `interview_invite` | **0.85**  | Moderate — false positive is recoverable (the user sees a "scheduled interview" status they didn't expect, edits to correct). False negative just goes to review. |
   | `recruiter_outreach` | **0.80** | Moderate-low — these emails don't change `Application.canonicalStatus`; the cost of mis-labeling is informational. |
   | `noise`            | **0.70**  | Aggressive — we WANT noise filtered. False positives sit in review queue with no downstream effect. False negatives clutter the user's view. |
   | `unmatched`        | **1.0**   | NEVER auto-acts. Always sent to review. The unmatched label is the explicit "I don't know" outcome. |

   Why a typed map, not env vars:
   - Per-label values must be set together (asymmetry is the contract).
   - Threshold changes are decisions that warrant code review + test changes — not
     an .env edit at runtime.
   - The map is type-checked: adding a new `EmailClassification` enum variant
     forces a TypeScript error in `thresholds.ts` until the new label is mapped.

   The pre-existing `env.CLASSIFIER_AUTO_THRESHOLD` (default 0.85) STAYS in
   `src/core/env.ts` for backward compatibility but is **not consumed by the
   act-stage gate**. Phase 4 may use it as a global *floor* (e.g., reject if
   any label's confidence is below 0.5 regardless of `THRESHOLDS[label]`),
   but it is NOT the discriminating value.

   #### Bonus — budget guard as control, not monitoring (Phase 3 CONTEXT §Area 4)

   Plan 03-01's `src/features/classifier/budget.ts` exports `checkBudget()`,
   which Plan 03-02's `classifyEmail` calls BEFORE every Anthropic SDK call
   (PITFALLS.md §6: pre-call check, not post-call accounting). The daily cap
   is `$0.50 USD`, computed from `data/classifier-log.jsonl` entries with
   today's `ts`. On budget exhaustion: `err({_tag: 'RateLimited', retryAfterSeconds: msUntilTomorrow})`.
   On read failure: **FAIL CLOSED** (also `RateLimited`) — never silently allow.

   Anthropic Claude Haiku 4.5 pricing constants (locked in `budget.ts`, comment-anchored to source date):
   - `HAIKU_INPUT_USD_PER_MTOK = 0.80`
   - `HAIKU_OUTPUT_USD_PER_MTOK = 4.00`

   Updates to pricing require a `budget.ts` edit + a test re-run (the pure
   `computeCostUsd` function is unit-tested with exact expected values).
   ```

5. Append to the existing `## References` section the following bullets (before the Supersedes section):
   ```markdown
   - `src/features/classifier/thresholds.ts` (Plan 03-01) — the typed THRESHOLDS map + meetsThreshold predicate. Authoritative spec for Decision C.
   - `src/features/classifier/budget.ts` (Plan 03-01) — checkBudget pre-call guard + appendCostEntry recorder. Authoritative spec for Decision C bonus.
   - `src/features/classifier/service.ts` (Plan 03-02) — composes rules → budget gate → LLM. Reference implementation for "budget gate runs before SDK call".
   - `.planning/phases/03-classifier-matcher/03-CONTEXT.md` §"Area 3" + §"Area 4" — the locked Phase 3 decisions Decision C records.
   - PITFALLS.md §4 — "First wrong auto-classification destroys trust before undo can save it". The driving rationale for asymmetry.
   ```

6. Update the `**Date**:` line at the top of the ADR — append `(amended 2026-05-09 — added Decision C)`.

7. Do NOT change any other content. Per CLAUDE.md §1.3 (surgical): only the new Sub-context C, the new Decision C, the appended References bullets, and the Date line are touched. Sections A and B remain word-for-word identical.

8. Verify ADR is well-formed: `grep -c "^## " docs/decisions/0012-status-regression-block-and-auto-update-styling.md` should return ≥6 (the existing 6: Context, Decision, Consequences, When we'd reconsider, References, Supersedes — all unchanged). Subsection count `grep -c "^### " ...` should be ≥5 (Sub-context A, B, C; Decision A, B, C — six subsections).
  </action>
  <verify>
    <automated>cd /Users/edwardpham/Documents/Programming/Projects/foray && [ "$(grep -c '^## ' docs/decisions/0012-status-regression-block-and-auto-update-styling.md)" -ge 6 ] && grep -c "Asymmetric per-label classifier thresholds" docs/decisions/0012-status-regression-block-and-auto-update-styling.md && grep -c "thresholds\.ts" docs/decisions/0012-status-regression-block-and-auto-update-styling.md | awk '$1 >= 3 {exit 0} {exit 1}' && grep -c "rejection.*0.92\|0.92.*rejection" docs/decisions/0012-status-regression-block-and-auto-update-styling.md && grep -c "interview_invite.*0.85\|0.85.*interview_invite" docs/decisions/0012-status-regression-block-and-auto-update-styling.md && grep -c "noise.*0.70\|0.70.*noise" docs/decisions/0012-status-regression-block-and-auto-update-styling.md && grep -c "unmatched.*1.0\|1.0.*unmatched" docs/decisions/0012-status-regression-block-and-auto-update-styling.md && grep -c "budget\.ts" docs/decisions/0012-status-regression-block-and-auto-update-styling.md && grep -c "FAIL CLOSED\|fail closed" docs/decisions/0012-status-regression-block-and-auto-update-styling.md && grep -c "amended 2026-05-09" docs/decisions/0012-status-regression-block-and-auto-update-styling.md</automated>
  </verify>
  <done>ADR-0012 has Decision C section + Sub-context C + Date amended; all 5 thresholds (0.92/0.85/0.80/0.70/1.0) appear; thresholds.ts referenced ≥3 times; budget.ts referenced; FAIL CLOSED documented; ADR ≥6 ## headings (no sections removed); existing Decision A and B sections unchanged (verify by spot-check).</done>
</task>

<task type="auto">
  <name>Task 2: Phase 3 UAT artifact + final pre-commit gate</name>
  <files>.planning/phases/03-classifier-matcher/03-UAT.md</files>
  <read_first>.planning/phases/02-applications-slice-manual-tracker/02-UAT.md (reference format); .planning/ROADMAP.md §"Phase 3" success criteria; .planning/REQUIREMENTS.md §"Classifier" + §"Matcher"</read_first>
  <action>
1. Create `.planning/phases/03-classifier-matcher/03-UAT.md` with the structured table format from 02-UAT.md. Sections:

   ```markdown
   # Phase 3 UAT — Classifier + Matcher

   **Status:** PRE-MERGE — owner walkthrough required before merging Phase 3 to main.
   **Created:** 2026-05-09 (Plan 03-05)

   This artifact records every Phase 3 verification item that needs owner action
   before the phase is closed. Mirrors Phase 2's 02-UAT.md pattern: in-code criteria
   are PASS now; manual verification items are DEFERRED.

   ## ROADMAP.md Phase 3 success criteria

   | # | Criterion | Status | Evidence |
   |---|-----------|--------|----------|
   | 1 | classifyEmail returns Result<{label, confidence, classifiedBy}, AppError> via rules-first then LLM fallback | PASS | Plan 03-02 service.test.ts (≥8 tests); rules.test.ts (≥30 tests) |
   | 2 | Per-label asymmetric thresholds in typed config (NOT env-var blanket) | PASS | thresholds.ts THRESHOLDS const + meetsThreshold predicate; ADR-0012 §C records |
   | 3 | Pre-call budget guard returns RateLimited at ≥$0.50/day; idempotency on email.classifiedBy in Phase 4 (out of scope here); per-batch cap in Phase 4 | PASS (in-scope parts) | budget.test.ts T3-T6 verify budget guard; idempotency + per-batch cap noted as Phase 4 contracts in 03-02-SUMMARY.md |
   | 4 | matcher/service.matchEmail() with thread→ATS-skip→domain→unmatched tiebreak; zero direct prisma | PASS | matcher-service.test.ts (≥6 tests); depcheck verifies no direct prisma in matcher/ |
   | 5 | Classifier-fixtures suite ≥1/label + ≥3 ATS samples; matcher tests cover 4 paths; ADR-0012 amendment committed | PASS | classifier-fixtures.test.ts loads ≥8 fixtures; matcher-service.test.ts has 4 path tests; ADR-0012 §C amended this plan |

   ## v1 requirement IDs (Phase 3 scope)

   | ID | Description | Status | Evidence |
   |----|-------------|--------|----------|
   | CLASS-01 | Rules-first classifier with externalized regex table | PASS | rules.ts + rules.test.ts (≥30 tests) + classifier-fixtures.test.ts (≥8) |
   | CLASS-02 | LLM fallback wrapped in Result; timeout 15s; maxRetries 0 | PASS | llm.ts + llm.test.ts (≥4 tests verifying SDK args) |
   | CLASS-03 | Per-label thresholds in typed config; NOT single env var | PASS | thresholds.ts + thresholds.test.ts (≥6 tests); ADR-0012 §C |
   | CLASS-04 | Anthropic calls logged to data/classifier-log.jsonl; pre-call budget guard | PASS | budget.ts + budget.test.ts (≥5 tests); appendCostEntry called by service.ts on LLM success |
   | MATCH-01 | matchEmail returns Result<{applicationId: ApplicationId \| null}, AppError> | PASS | matcher/service.ts + matcher-service.test.ts |
   | MATCH-02 | Tiebreak: thread → domain (skipping ATS) → unmatched | PASS | matcher-service.test.ts T1-T4 + T9 (subdomain ATS) |
   | MATCH-03 | All Prisma access via tenantDb (no direct imports) | PASS | depcheck Rule 4 (no-direct-prisma); grep verification in matcher/ |

   ## Manual / pre-merge verification items (owner walkthrough required)

   | ID | What to verify | Status | Notes |
   |----|----------------|--------|-------|
   | UAT-03-01 | Set `ANTHROPIC_API_KEY` in local `.env` (required by env.ts after Plan 03-01 promoted it) | DEFERRED | Get from https://console.anthropic.com/settings/keys ; without it, `pnpm build` fails at env validation (which IS the desired behavior — fail loud). |
   | UAT-03-02 | Run `pnpm build` — confirm it succeeds with ANTHROPIC_API_KEY set | DEFERRED | Validates env wiring end-to-end. |
   | UAT-03-03 | (Optional, costs ~$0.0005) Run a single real LLM classification via a one-off script: write a temp script that imports classifyEmail and calls it with a sample email; confirm `data/classifier-log.jsonl` is created and has 1 entry with the expected shape | DEFERRED | Owner-discretion smoke test. Not required for merge — the integration tests already mock the SDK end-to-end. |
   | UAT-03-04 | Inspect `data/classifier-log.jsonl` after UAT-03-03: confirm row has `ts, model, inputTokens, outputTokens, costUsd, emailHash` keys; emailHash starts with `sha256:`; NO raw subject or body present (privacy regression check) | DEFERRED | If raw content appears, that's a Plan 03-02 bug — file an issue and revert. |
   | UAT-03-05 | (Optional) Add 1 real anonymized email per label to `tests/integration/classifier-fixtures/<label>/`; re-run `pnpm test:run` to confirm fixtures match expected labels (or refine rules.ts as documented in the fixture README) | DEFERRED | Plan 03-04 ships synthetic fixtures; replacing with real samples improves rule precision. Optional for merge. |

   ## Cross-cutting trust trio status (per ROADMAP.md)

   | Concern | Status | Where it lives |
   |---------|--------|----------------|
   | Per-label classifier thresholds | **Locked** | thresholds.ts + ADR-0012 §C (Plan 03-05) |
   | Status-regression block | Locked (Phase 2) | applications/service.ts:applyAutoStatusChange + ADR-0012 §A |
   | Visually-distinct auto-update events | Locked (Phase 2) | timeline.tsx + ADR-0012 §B |
   | Budget guard (control, not monitoring) | **Locked** | budget.ts:checkBudget + ADR-0012 §C bonus (Plan 03-05) |
   | Idempotency on email.classifiedBy | Phase 4 | classifier service is idempotency-AGNOSTIC; Phase 4's pollOnce checks email.classifiedBy before calling. Documented in 03-02-SUMMARY.md. |
   | Per-batch hard cap (50 emails) | Phase 4 | Phase 3 doesn't loop. Phase 4's pollOnce will. Documented in 03-02-SUMMARY.md. |

   ## Final pre-commit gate

   Re-run as the LAST action of Plan 03-05:

   ```bash
   pnpm lint && pnpm typecheck && pnpm test:run && pnpm build && pnpm depcheck
   ```

   Result: PASS — all green. (Date + test count + route count to be filled in by the executor at commit time.)

   ## Phase 4 readiness

   Phase 4 (`inbox/` slice) imports the following from Phase 3:

   ```ts
   import { classifyEmail } from '@/features/classifier/service'
   import { THRESHOLDS, meetsThreshold } from '@/features/classifier/thresholds'
   import { matchEmail } from '@/features/matcher/service'
   ```

   The `dependency-cruiser` exception for `inbox/` importing `matcher/service.ts` and `classifier/service.ts` already exists (`.dependency-cruiser.cjs` lines 21-30) — no additional config changes needed.

   ADR-0012 §C is the authoritative spec for the auto-act gate.

   ---

   *Phase 3 UAT artifact created by Plan 03-05 on 2026-05-09. Owner walkthrough required for items marked DEFERRED before merging Phase 3 to main.*
   ```

2. Verify the file has ≥10 PASS|FAIL|DEFERRED tokens by `grep -cE 'PASS|FAIL|DEFERRED'`. Expected: ≥17 (5 ROADMAP + 7 requirements + 5 manual = 17 status rows + the ≥6 trust-trio rows). Floor is 10.

3. **Run the FINAL pre-commit gate** — this is the last gate of the entire phase:
   ```bash
   pnpm lint && pnpm typecheck && pnpm test:run && pnpm build && pnpm depcheck
   ```
   All MUST pass. Capture the test count, route count, and any warnings into the `## Final pre-commit gate` section above (replace the placeholder line with actuals).

4. Do NOT touch STATE.md, ROADMAP.md, or REQUIREMENTS.md — those are the orchestrator's responsibility per the standard close-out pattern (matches Plan 02-05 line 211 of 02-05-SUMMARY.md).

Per CLAUDE.md §1.2 (simplicity): the UAT is structured but minimal. No "test plan" speculation, no "regression testing schedule", no "rollback runbook". Just the 17-ish rows that map to actual verifications.

Per CLAUDE.md §1.3 (surgical): only ONE file is created (03-UAT.md). Task 1 modifies one ADR. Total Phase 3 touch surface for Plan 05: 2 files. Plan 02-05 set the same precedent (2 files).
  </action>
  <verify>
    <automated>cd /Users/edwardpham/Documents/Programming/Projects/foray && [ -f .planning/phases/03-classifier-matcher/03-UAT.md ] && [ "$(wc -l < .planning/phases/03-classifier-matcher/03-UAT.md)" -ge 30 ] && [ "$(grep -cE 'PASS|FAIL|DEFERRED' .planning/phases/03-classifier-matcher/03-UAT.md)" -ge 10 ] && grep -c "CLASS-01\|CLASS-02\|CLASS-03\|CLASS-04" .planning/phases/03-classifier-matcher/03-UAT.md | awk '$1 >= 4 {exit 0} {exit 1}' && grep -c "MATCH-01\|MATCH-02\|MATCH-03" .planning/phases/03-classifier-matcher/03-UAT.md | awk '$1 >= 3 {exit 0} {exit 1}' && grep -c "ANTHROPIC_API_KEY" .planning/phases/03-classifier-matcher/03-UAT.md && grep -c "classifier-log\.jsonl" .planning/phases/03-classifier-matcher/03-UAT.md && pnpm lint && pnpm typecheck && pnpm test:run && pnpm build && pnpm depcheck</automated>
  </verify>
  <done>03-UAT.md ≥30 lines + ≥10 PASS|FAIL|DEFERRED tokens; covers all 7 requirements + 5 success criteria + ≥4 manual items; final pre-commit gate green; STATE/ROADMAP/REQUIREMENTS untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ADR document → future Phase 4 reader | ADR-0012 is the contract; Phase 4 must NOT re-derive from CONTEXT.md |
| 03-UAT.md → owner pre-merge walkthrough | DEFERRED rows are the close-out gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | Repudiation | ADR drift | mitigate | ADR-0012 amendment is in-place because the original ADR is recent and the amendment IS its natural completion (the original explicitly deferred the threshold rationale to Phase 3). Future amendments require supersession (per ADR-0011 convention). The "amended 2026-05-09" date stamp + Decision C section make the change discoverable via git blame. |
| T-03-05-02 | Information Disclosure | UAT artifact in version control | accept | 03-UAT.md is committed to .planning/ and visible to anyone with repo access. No PII or secrets are recorded — only verification status. Same pattern as Phase 2's 02-UAT.md. |
| T-03-05-03 | Tampering | UAT incompleteness | accept | If a manual verification step is missed (e.g., owner skips UAT-03-04 privacy check), a regression could ship. Acceptable for Lean — the owner is also the QA. Standard milestone may add CI-driven privacy checks (e.g., grep classifier-log.jsonl for raw email content patterns). |
| T-03-05-04 | Spoofing | "amended" marker hides supersession | accept | Marking the ADR as "amended" rather than superseding it is a deliberate exception per the precedent in this milestone. Future amendments require new ADRs. Documented in this plan's existing_patterns block + Task 1 step 7. |
</threat_model>

<verification>
After both tasks:
1. ADR-0012 has new Decision C section with all 5 thresholds in the rationale table
2. 03-UAT.md ≥10 PASS|FAIL|DEFERRED tokens; ≥30 lines; covers all 7 requirements
3. Final pre-commit gate green: `pnpm lint && pnpm typecheck && pnpm test:run && pnpm build && pnpm depcheck`
4. Test count after this plan = test count before (no test changes; only docs)
5. STATE.md / ROADMAP.md / REQUIREMENTS.md NOT modified (verify with `git status` — only ADR + UAT should be staged)
6. No ESLint warnings introduced. Build artifacts unchanged from Plan 03-04 baseline.
</verification>

<success_criteria>
- ADR-0012 §C records the asymmetric thresholds + budget guard rationale
- 03-UAT.md persists every Phase 3 verification item with PASS/FAIL/DEFERRED status
- Phase 4 has a single grep-discoverable spec for the auto-act gate (`THRESHOLDS` + `meetsThreshold` + ADR-0012 §C)
- Final pre-commit gate green — Phase 3 ships at the code layer
- Owner has a structured walkthrough list for pre-merge UAT (mirrors Phase 1 + Phase 2 pattern)
</success_criteria>

<output>
After completion, create `.planning/phases/03-classifier-matcher/03-05-SUMMARY.md` per the template. Include:
- ADR-0012 §C section heading + summary of the rationale
- 03-UAT.md row counts (ROADMAP / requirements / manual / trust trio)
- Final pre-commit gate status: lint ✓ / typecheck ✓ / tests N passing / build M routes / depcheck clean
- Phase 3 close-out status: COMPLETE (code layer); pre-merge UAT walkthrough deferred to owner
- Phase 4 readiness: ADR-0012 + the import block ready
</output>
