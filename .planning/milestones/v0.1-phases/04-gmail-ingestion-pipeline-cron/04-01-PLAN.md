---
phase: 04-gmail-ingestion-pipeline-cron
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/
autonomous: true
requirements: [GMAIL-03]

must_haves:
  truths:
    - "Email model has a processing_status column with enum values: received, matched, classified, acted, needs_review, failed"
    - "User model has a gmailHistoryId field for storing the Gmail history list watermark"
    - "Schema push succeeds against the local database"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "ProcessingStatus enum + processing_status on Email + gmailHistoryId on User"
      contains: "ProcessingStatus"
  key_links:
    - from: "prisma/schema.prisma"
      to: "Email.processing_status"
      via: "enum + column definition"
      pattern: "processing_status.*ProcessingStatus"
---

<objective>
Add the `ProcessingStatus` enum and `processing_status` column to the `Email` model, plus `gmailHistoryId` to the `User` model. These two schema changes are prerequisites for the entire Phase 4 pipeline — without `processing_status`, the 4-stage pipeline cannot track per-email progress; without `gmailHistoryId`, the ingest stage has no watermark to resume from.

Purpose: Unblocks all downstream plans (OAuth, pipeline, act, cron).
Output: Updated `prisma/schema.prisma` + applied migration via `npx prisma db push`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-gmail-ingestion-pipeline-cron/04-RESEARCH.md
@prisma/schema.prisma
@PRINCIPLES.md (§"Email pipeline — 4 idempotent stages" for ProcessingStatus state machine)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ProcessingStatus enum + processing_status column to Email model</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    - prisma/schema.prisma (current schema — verify Email model structure, existing enums)
    - PRINCIPLES.md §"Email pipeline — 4 idempotent stages" (state machine: received -> matched -> classified -> acted | needs_review | failed)
  </read_first>
  <action>
Add a new `ProcessingStatus` enum to `prisma/schema.prisma` after the existing enums block (after `DocumentKind`):

```prisma
enum ProcessingStatus {
  received
  matched
  classified
  acted
  needs_review
  failed
}
```

Add a `processing_status` field to the `Email` model, placed after the `bodyExcerpt` field (before `receivedAt`):

```prisma
processingStatus ProcessingStatus @default(received) @map("processing_status")
```

Add an index on the new column (after the existing `@@index([receivedAt])` line):

```prisma
@@index([processingStatus])
```

The `received` default means newly ingested emails start at stage 1 of the pipeline. The pipeline stages advance the status as each stage completes. `needs_review` is set when confidence is below threshold or no application matched. `failed` is set on permanent errors.
  </action>
  <verify>
    <automated>grep -n "ProcessingStatus" prisma/schema.prisma && grep -n "processing_status" prisma/schema.prisma</automated>
  </verify>
  <acceptance_criteria>
    - prisma/schema.prisma contains `enum ProcessingStatus {` with values: received, matched, classified, acted, needs_review, failed
    - Email model contains `processingStatus ProcessingStatus @default(received) @map("processing_status")`
    - Email model contains `@@index([processingStatus])`
    - `npx prisma format` exits 0
  </acceptance_criteria>
  <done>ProcessingStatus enum and processing_status column exist on Email model with correct default and index</done>
</task>

<task type="auto">
  <name>Task 2: Add gmailHistoryId to User model + schema push</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    - prisma/schema.prisma (current User model — verify existing Gmail fields)
    - 04-RESEARCH.md §"Open Questions #1" (gmailHistoryId stores the history.list watermark, separate from gmailLastSyncAt which tracks *when*)
  </read_first>
  <action>
Add a `gmailHistoryId` field to the `User` model in `prisma/schema.prisma`, placed after the existing `gmailLastSyncAt` field:

```prisma
gmailHistoryId            String?   @map("gmail_history_id")
```

`gmailLastSyncAt` tracks *when* the last sync happened (for the 5-day health banner in GMAIL-02). `gmailHistoryId` tracks *where* in Gmail's history to resume (the `startHistoryId` for `history.list`). They serve different purposes — see RESEARCH.md Open Questions #1.

After all schema changes are complete, run:

```bash
npx prisma db push
```

This applies the schema changes to the local database. If the database is not running, start it first with `docker compose up -d db`.
  </action>
  <verify>
    <automated>grep -n "gmailHistoryId" prisma/schema.prisma && npx prisma db push --accept-data-loss 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - prisma/schema.prisma User model contains `gmailHistoryId String? @map("gmail_history_id")`
    - prisma/schema.prisma User model contains `gmailLastSyncAt DateTime? @map("gmail_last_sync_at")` (pre-existing, not removed)
    - `npx prisma db push` exits 0 (database reflects schema)
    - `npx prisma generate` exits 0 (generated client includes new fields)
  </acceptance_criteria>
  <done>User model has gmailHistoryId field; schema pushed to database; generated client updated</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema → DB | Schema changes applied via prisma db push — destructive if data exists |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Avail | Email.processing_status | accept | Default `received` is correct; existing rows get the default on push. No data loss risk since Email table is empty pre-Phase 4. |
</threat_model>

<verification>
- `npx prisma db push` succeeds
- `npx prisma generate` succeeds
- `pnpm typecheck` passes (new fields are optional/defaulted, no consumer code yet)
</verification>

<success_criteria>
- ProcessingStatus enum with 6 values exists in schema
- Email.processing_status column exists with `received` default
- User.gmailHistoryId column exists
- Database reflects all changes
</success_criteria>

<output>
After completion, create `.planning/phases/04-gmail-ingestion-pipeline-cron/04-01-SUMMARY.md`
</output>
