---
phase: 01-foundation-auth
plan: 04
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
  - "01-03"
files_modified:
  - docs/decisions/0011-rls-via-with-rls-helper.md
  - .planning/PROJECT.md
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "ADR-0011 (RLS via withRls helper, not Prisma client extension, until SaaS flip) is committed to docs/decisions/ — locks the Phase 1 decision so Phase 2-5 don't re-litigate it"
    - ".planning/PROJECT.md lines 99 and 121 say 'Next.js 16' (not 'Next.js 15') — the documented stack matches package.json (16.2.6)"
    - "Full pre-commit gate green: pnpm lint && pnpm typecheck && pnpm test:run && pnpm build && pnpm depcheck — FND-04 sentinel check (subset of FND-04; full FND-04 verification lands in Phase 5)"
  artifacts:
    - path: "docs/decisions/0011-rls-via-with-rls-helper.md"
      provides: "ADR-0011 — locks Phase 1 RLS implementation choice with consequences + reconsideration triggers"
      contains: "withRls"
  key_links:
    - from: "docs/decisions/0011-rls-via-with-rls-helper.md"
      to: "src/core/db/with-rls.ts"
      via: "ADR documents the actual shipped helper"
      pattern: "withRls"
    - from: "docs/decisions/0011-rls-via-with-rls-helper.md"
      to: ".planning/research/ARCHITECTURE.md"
      via: "References block cites the research that locked the decision"
      pattern: "ARCHITECTURE"
---

<objective>
Close Phase 1: write the ADR that locks the `withRls` decision, fix the two `.planning/PROJECT.md` lines that say "Next.js 15" (installed is 16.2.6 — surgical 2-line edit), and run the full pre-commit gate one more time as the FND-04 sentinel check.

Purpose: ADR-0011 is one of Phase 1's five success criteria from ROADMAP.md. The PROJECT.md doc fix is locked in CONTEXT.md as in-scope. The pre-commit gate run is the gating signal that Phase 1 actually shipped clean — not just "the code looks done."

This plan must run AFTER Plans 01, 02, 03 because:
- ADR-0011 references the actual shipped `withRls` helper (Plan 01) and the migration (Plan 03)
- The pre-commit gate run depends on every other plan's code being landed
- The PROJECT.md fix is independent but cheap to bundle here

Output:
- `docs/decisions/0011-rls-via-with-rls-helper.md` — Nygard-format ADR (~70 lines)
- `.planning/PROJECT.md` — 2 lines changed ("Next.js 15" → "Next.js 16")
- Confirmed pre-commit gate green (no file modified — verification only)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
@CLAUDE.md
@docs/decisions/0010-architecture-vertical-slice.md

<interfaces>
<!-- Plan 01-03 prerequisites: the ADR references shipped behavior. -->

From src/core/db/with-rls.ts (Plan 01):
```typescript
export function withRls<T>(
  userId: UserId,
  fn: (tx: Prisma.TransactionClient) => Promise<T>,
): Promise<Result<T, AppError>>
```

From src/core/db/tenant.ts (after Plan 03 Task 2):
```typescript
export function tenantDb(userId: UserId)  // FND-01 matrix landed
```

From prisma/migrations/<ts>_add_rls_and_app_role/migration.sql (Plan 03):
- ENABLE + FORCE ROW LEVEL SECURITY on 9 tenant-scoped tables
- tenant_isolation policies via current_setting('app.user_id', true)
- foray_app non-superuser role with CRUD grants

ADR style reference (read docs/decisions/0010-architecture-vertical-slice.md):
- Status / Date / Context / Decision / Consequences / References / Supersedes layout

PROJECT.md target lines:
- Line 99: `- Next.js 15 App Router + React 19 + TypeScript (strict)`
- Line 121: `**Tech stack**: Next.js 15 + Prisma 7 + Postgres in Docker — no cloud DB, no Vercel deploy in Lean (local-first per ADR-0003)`
Both change `Next.js 15` → `Next.js 16`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write ADR-0011 (RLS via withRls helper, not Prisma client extension, until SaaS flip)</name>
  <files>docs/decisions/0011-rls-via-with-rls-helper.md</files>
  <read_first>
    - docs/decisions/0010-architecture-vertical-slice.md (verify Nygard format: Status/Date/Context/Decision/Consequences/References/Supersedes)
    - .planning/phases/01-foundation-auth/01-RESEARCH.md §6 "ADR-0011 wording skeleton" (lines 742-826) — VERBATIM template
    - src/core/db/with-rls.ts (Plan 01) — confirm exact function signature and pattern
    - prisma/migrations/<ts>_add_rls_and_app_role/migration.sql (Plan 03) — confirm FORCE + tenant_isolation policy patterns
    - .planning/research/ARCHITECTURE.md §"Where RLS hooks in (concrete) — Step 3" — research that locked the decision
    - .planning/research/PITFALLS.md §"Pitfall 2" — failure modes prevented
  </read_first>
  <action>
    Create `docs/decisions/0011-rls-via-with-rls-helper.md` using the VERBATIM template from RESEARCH.md §6 (lines 746-826). Match the exact section ordering of ADR-0010 (Status / Date / Context / Decision / Consequences with Positive+Negative subsections / When we'd reconsider / References / Supersedes).

    Use today's date in the Date field: `**Date**: 2026-05-09`. Set `**Status**: Accepted`.

    Critical sections to include verbatim from RESEARCH.md §6:
    - **Context** — names both options ($extends vs withRls), cites the three concrete reasons against $extends (round-trip doubling; Prisma Issue #23583; tenantDb test conflicts; review-driven safety vs SaaS untrusted-code-paths)
    - **Decision** — locks `withRls(userId, async tx => …)` for multi-statement; `tenantDb(userId)` for single-row reads; explicit "Do NOT add `$extends({ query })` to core/db/client.ts for Lean"
    - **Consequences > Positive** — no double round-trip; no test conflict; avoids Issue #23583; explicit callsite intent
    - **Consequences > Negative** — two callsite shapes (mitigated by README + dep-cruiser rule); contributor-unfamiliar-with-split risk (mitigated by RLS-deny-zero-rows fail-fast)
    - **When we'd reconsider** — SaaS flip; Prisma fixes #23583; connection-pool exhaustion under multi-user load
    - **References** — link to:
      - `.planning/research/ARCHITECTURE.md` §"Where RLS hooks in (concrete) — Step 3"
      - `.planning/research/PITFALLS.md` §"Pitfall 2"
      - https://github.com/prisma/prisma/issues/23583
      - https://github.com/prisma/prisma/issues/17948
      - https://www.postgresql.org/docs/current/ddl-rowsecurity.html
    - **Supersedes**: None. Establishes the RLS implementation pattern locked by Phase 1.

    Markdown style: H1 is `# ADR-0011: RLS via withRls() helper, not Prisma client extension, until SaaS flip` (verify ADR-0010 uses this exact H1 pattern — match it).

    Length target: ~70-80 lines. The text is locked by RESEARCH.md; do not add speculative sections. (Karpathy 1.2 — minimum viable.)
  </action>
  <verify>
    <automated>
      test -f docs/decisions/0011-rls-via-with-rls-helper.md && \
      grep -q "ADR-0011" docs/decisions/0011-rls-via-with-rls-helper.md && \
      grep -q "withRls" docs/decisions/0011-rls-via-with-rls-helper.md && \
      grep -q "Status" docs/decisions/0011-rls-via-with-rls-helper.md && \
      grep -q "Decision" docs/decisions/0011-rls-via-with-rls-helper.md && \
      grep -q "Consequences" docs/decisions/0011-rls-via-with-rls-helper.md && \
      grep -q "23583" docs/decisions/0011-rls-via-with-rls-helper.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `ls docs/decisions/0011-*.md` returns one file
    - `grep -c "ADR-0011" docs/decisions/0011-rls-via-with-rls-helper.md` returns ≥1
    - `grep -c "withRls" docs/decisions/0011-rls-via-with-rls-helper.md` returns ≥4 (mentioned in title + decision + at least 2 elsewhere)
    - `grep -q "## Context\|## Decision\|## Consequences" docs/decisions/0011-rls-via-with-rls-helper.md` exits 0
    - `grep -q "23583" docs/decisions/0011-rls-via-with-rls-helper.md` exits 0 (Prisma issue cited)
    - `grep -q "ARCHITECTURE.md\|PITFALLS.md" docs/decisions/0011-rls-via-with-rls-helper.md` exits 0
    - `wc -l docs/decisions/0011-rls-via-with-rls-helper.md` returns 50-120 lines
  </acceptance_criteria>
  <done>
    ADR-0011 committed to docs/decisions/. Status: Accepted. References point to research files + Prisma Issue #23583 + Postgres docs. Locks Phase 1's withRls vs $extends decision.
  </done>
</task>

<task type="auto">
  <name>Task 2: Surgical-fix .planning/PROJECT.md Next.js 15 → Next.js 16 (lines 99 + 121)</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md lines 95-125 (verify both lines 99 + 121 still say "Next.js 15"; the file may have shifted lines since RESEARCH.md was written)
    - package.json (line 42 confirms `"next": "16.2.6"`)
    - .planning/phases/01-foundation-auth/01-RESEARCH.md §7 "Doc fix detail" (lines 830-839)
    - CLAUDE.md §1.3 "Surgical Changes" — touch only the two lines, no reformatting
  </read_first>
  <action>
    Step A — Confirm both lines still need fixing:
    ```bash
    grep -n "Next.js 15" .planning/PROJECT.md
    ```
    Expect: 2 matches (or possibly fewer if line drift; the change scope is "every line that says Next.js 15 in PROJECT.md", not specifically lines 99 and 121).

    Step B — Surgical replace ONLY in `.planning/PROJECT.md`. Two edits:
    - `Next.js 15 App Router + React 19 + TypeScript (strict)` → `Next.js 16 App Router + React 19 + TypeScript (strict)`
    - `Next.js 15 + Prisma 7 + Postgres in Docker` → `Next.js 16 + Prisma 7 + Postgres in Docker`

    DO NOT touch:
    - `.planning/research/PITFALLS.md` (RESEARCH.md §7 explicitly: research files are timestamped — flag in commit message, do not silently fix)
    - Any other file (CLAUDE.md, README.md, docs/architecture.md) unless they ALSO say "Next.js 15" — but RESEARCH.md only locks PROJECT.md as in-scope per CONTEXT.md decisions section. Out-of-scope per Karpathy 1.3.

    Step C — In the commit message body for this task, add one line: "Note: PITFALLS.md line 502 also says 'Next.js 15' but is intentionally not fixed here — research files are timestamped artifacts. Flag for future cleanup."
  </action>
  <verify>
    <automated>
      ! grep -q "Next.js 15" .planning/PROJECT.md && \
      grep -c "Next.js 16" .planning/PROJECT.md | awk '{exit ($1>=2)?0:1}'
    </automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q "Next.js 15" .planning/PROJECT.md` exits 0 (no remaining "Next.js 15" string)
    - `grep -c "Next.js 16" .planning/PROJECT.md` returns ≥2
    - Other files unchanged: `git diff --name-only HEAD .planning/PROJECT.md` is the only file in this task's diff
  </acceptance_criteria>
  <done>
    .planning/PROJECT.md no longer says "Next.js 15"; both occurrences replaced with "Next.js 16" via surgical 2-line edit. Other files untouched.
  </done>
</task>

<task type="auto">
  <name>Task 3: FND-04 sentinel — full pre-commit gate must pass on Phase 1's combined output</name>
  <files></files>
  <read_first>
    - CLAUDE.md §2.1 (Pre-commit checklist — non-negotiable, no --no-verify)
    - .planning/REQUIREMENTS.md FND-04 (full sentinel; this task is a Phase 1 subset confirming we ship clean)
    - package.json (verify scripts: lint, typecheck, test:run, build, depcheck all present)
  </read_first>
  <action>
    Run the full pre-commit gate exactly once on the combined Phase 1 output (Plan 01 + Plan 02 + Plan 03 + this plan's ADR + PROJECT.md fix):

    ```bash
    pnpm lint && pnpm typecheck && pnpm test:run && pnpm build && pnpm depcheck
    ```

    All five MUST exit 0. If any fails:
    1. Identify which plan introduced the regression (Plan 01 / 02 / 03 most likely)
    2. Fix in this plan (do not re-open prior plans — Phase 1 ships as a unit)
    3. Re-run the full gate
    4. NEVER use `--no-verify` (CLAUDE.md §2.1)

    If `pnpm test:run` is slow because of Testcontainers (10-15s container boot per fresh run), that's expected — verify via `time` once:
    ```bash
    time pnpm test:run
    ```
    Target: <60 seconds total for the integration suite plus unit tests. If it exceeds 2 minutes, flag in the SUMMARY (not a Phase 1 blocker; Phase 5 hardening can revisit).

    This task creates NO files. It is pure verification. The verify block is the success criterion.
  </action>
  <verify>
    <automated>
      pnpm lint && pnpm typecheck && pnpm test:run && pnpm build && pnpm depcheck
    </automated>
  </verify>
  <acceptance_criteria>
    - `pnpm lint` exits 0 (no ESLint errors)
    - `pnpm typecheck` exits 0 (no tsc --noEmit errors)
    - `pnpm test:run` exits 0 (all unit + integration tests pass; encryption.test.ts + rls-escape.test.ts + tenant-db-cross-tenant-leak.test.ts all green)
    - `pnpm build` exits 0 (next build succeeds with the new /login page + middleware)
    - `pnpm depcheck` exits 0 (dep-cruiser passes, including the new inbox-pipeline-exception)
    - The commit chain is clean: `git status` shows the ADR file + PROJECT.md as the only changes in this plan's commit
  </acceptance_criteria>
  <done>
    Phase 1's combined output passes the full pre-commit gate. No --no-verify used. FND-04 sentinel green for Phase 1's surface (full FND-04 verification — Server-Action-returns-Result lint — lands in Phase 5 when there are enough Server Actions to make the rule worth writing).
  </done>
</task>

</tasks>

<verification>
After all 3 tasks complete, the Phase 1 success criteria from ROADMAP.md should ALL be satisfied:

1. Owner can log in via /login with the single password and is redirected to /applications — Plan 02 ✓
2. requireUser() reads iron-session cookie and returns Result<{id: UserId}, Unauthorized> — Plan 02 ✓
3. tenantDb exposes wrapped CRUD methods + withRls helper — Plans 01 + 03 ✓
4. RLS policies active on every tenant-scoped table; non-superuser role; escape-attempt query returns zero rows — Plan 03 ✓
5. ADR-0011 candidate committed to docs/decisions/ — this plan ✓

Run a final cross-cutting check that all five criteria are observable:
```bash
ls docs/decisions/0011-*.md && \
grep -q "Next.js 16" .planning/PROJECT.md && \
grep -q "set_config('app.user_id'" src/core/db/with-rls.ts && \
grep -q "FORCE ROW LEVEL SECURITY" prisma/migrations/*/migration.sql && \
grep -q "getIronSession" src/core/auth/session.ts && \
test -f src/middleware.ts && \
pnpm test:run -- tests/integration/rls-escape.test.ts
```
All exit codes 0 = Phase 1 ships.
</verification>

<success_criteria>
- [ ] `docs/decisions/0011-rls-via-with-rls-helper.md` exists with Nygard-format sections (Status / Date / Context / Decision / Consequences / References / Supersedes)
- [ ] ADR cites Prisma Issue #23583 + ARCHITECTURE.md + PITFALLS.md
- [ ] `.planning/PROJECT.md` contains zero occurrences of "Next.js 15" and ≥2 of "Next.js 16"
- [ ] No other files modified by this plan (surgical scope)
- [ ] Full pre-commit gate green: `pnpm lint && pnpm typecheck && pnpm test:run && pnpm build && pnpm depcheck` all exit 0
- [ ] All 5 Phase 1 ROADMAP.md success criteria observable in repo state
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-04-SUMMARY.md` capturing:
- ADR-0011 file path + line count
- PROJECT.md lines fixed (2 occurrences of "Next.js 15" → "Next.js 16")
- Pre-commit gate timing (lint / typecheck / test:run / build / depcheck — total seconds)
- Phase 1 status: ALL 5 success criteria observable
- Outstanding flagged (not blocking): PITFALLS.md line 502 still says "Next.js 15" — research file is timestamped, intentionally not fixed; queue as cleanup candidate
- Downstream consumers: Phase 2 starts from a green base; every Server Action created in Phase 2 onward must call `await requireUser()` as line 1 + use `tenantDb` or `withRls` for all Prisma access
</output>
