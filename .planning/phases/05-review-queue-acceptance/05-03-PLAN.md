---
phase: 05-review-queue-acceptance
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - scripts/check-server-actions.ts
  - .dependency-cruiser.cjs
  - docs/milestones/lean.md
autonomous: false
requirements: [FND-03, FND-04, REVIEW-01, REVIEW-02]

must_haves:
  truths:
    - "Every Server Action file exports functions that return Result<T, AppError> or ActionState (which contains ok: boolean)"
    - "relforcerowsecurity = true is verified in test setup for every tenant-scoped table"
    - "All 6 FND-03 test categories pass: (a) tenant isolation, (b) classifier fixtures, (c) matcher tiebreak, (d) auto-update + undo, (e) budget guard, (f) env validation"
    - "Pre-commit gate passes: pnpm lint && pnpm typecheck && pnpm test:run && pnpm build && pnpm depcheck"
    - "All 12 Lean acceptance criteria are verified"
  artifacts:
    - path: "scripts/check-server-actions.ts"
      provides: "Structural check: Server Actions return Result or ActionState"
      min_lines: 30
    - path: "docs/milestones/lean.md"
      provides: "Updated acceptance criteria with category coverage replacing count target"
      contains: "category"
  key_links:
    - from: "scripts/check-server-actions.ts"
      to: "src/features/*/actions.ts"
      via: "grep for 'use server' + return type analysis"
      pattern: "actions\\.ts"
    - from: ".dependency-cruiser.cjs"
      to: "src/features/inbox/"
      via: "Module boundary rules for inbox cross-slice import exception"
      pattern: "inbox.*matcher.*classifier"
---

<objective>
Wire structural CI checks that prevent regression of safety properties, verify FND-03 category-based test coverage, and walk through all 12 Lean acceptance criteria end-to-end.

Purpose: This is the hardening pass — ensuring the safety properties (RLS, Server-Action discipline, test coverage) are codified in CI, not just in documentation. Also updates the Lean milestone doc to reflect category coverage replacing the gameable "30 tests" count.
Output: 1 structural check script + 1 updated milestone doc + Lean acceptance verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-review-queue-acceptance/05-CONTEXT.md
@.planning/phases/05-review-queue-acceptance/05-RESEARCH.md
@.planning/phases/05-review-queue-acceptance/05-01-SUMMARY.md
@.planning/phases/05-review-queue-acceptance/05-02-SUMMARY.md
@docs/milestones/lean.md

<interfaces>
<!-- Existing test files for FND-03 category audit -->
From tests/integration/rls-escape.test.ts:
```typescript
// FND-03(a): Tenant isolation — RLS escape attempt with foreign user_id
```

From tests/integration/classifier-fixtures.test.ts:
```typescript
// FND-03(b): Classifier fixtures — at least one fixture per label
```

From tests/integration/matcher-service.test.ts:
```typescript
// FND-03(c): Matcher — all 4 tiebreak paths tested
```

From tests/integration/act-stage.test.ts:
```typescript
// FND-03(d): Auto-update + undo race — 7 tests covering gates
```

From src/features/classifier/budget.test.ts:
```typescript
// FND-03(e): Budget guard — runaway loop blocks 51st call
```

From src/core/env.test.ts:
```typescript
// FND-03(f): Env validation — every required field rejects missing/malformed
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FND-03 category coverage audit + FND-04 structural checks</name>
  <files>scripts/check-server-actions.ts, .dependency-cruiser.cjs</files>
  <read_first>
    tests/integration/rls-escape.test.ts,
    tests/integration/classifier-fixtures.test.ts,
    tests/integration/matcher-service.test.ts,
    tests/integration/act-stage.test.ts,
    src/features/classifier/budget.test.ts,
    src/core/env.test.ts,
    src/features/inbox/actions.ts,
    src/features/applications/actions.ts,
    .dependency-cruiser.cjs,
    src/instrumentation.ts
  </read_first>
  <action>
    **1. FND-03 Category Coverage Audit** — verify all 6 categories have passing tests:

    Run `pnpm test:run` and check each category:

    (a) **Tenant isolation via RLS escape**: `tests/integration/rls-escape.test.ts` — verify it tests that queries with a foreign user_id return zero rows. If missing tests for any tenant-scoped model (Application, Email, Stage, Event, Company), add them.

    (b) **Classifier fixtures per label**: `tests/integration/classifier-fixtures.test.ts` — verify at least 1 fixture per label (rejection, interview_invite, recruiter_outreach, noise, unmatched). The fixtures live in `tests/integration/classifier-fixtures/` subdirectories.

    (c) **Matcher per tiebreak path**: `tests/integration/matcher-service.test.ts` — verify tests cover: thread-continuity, sender-domain match, ATS-domain skip, unmatched fallback.

    (d) **Auto-update + undo race**: `tests/integration/act-stage.test.ts` — verify tests cover: threshold gate, match gate, regression gate, first-50 grace, undo idempotency (reviewedByUser=true skips act), advisory lock.

    (e) **Budget guard runaway loop**: `src/features/classifier/budget.test.ts` — verify it tests that the 51st call in a day is blocked.

    (f) **Env validation per required field**: `src/core/env.test.ts` — verify it tests each required env var (DATABASE_URL, ENCRYPTION_KEY, APP_PASSWORD, APP_SESSION_SECRET, ANTHROPIC_API_KEY) rejects missing/malformed input.

    For any gaps found, add the missing test to the existing test file (do NOT create new test files). Document findings in the summary.

    **2. Server-Action-returns-Result structural check** — create `scripts/check-server-actions.ts`:

    A Node.js script that:
    1. Uses `fs.readdirSync` to find all `**/actions.ts` files under `src/features/`
    2. For each file, reads the content
    3. Checks that every exported async function either:
       - Has a return type annotation containing `Result` or `ActionState`
       - OR the function body contains `return { ok:` or `return ok(` or `return err(`
    4. Prints PASS/FAIL per file with details
    5. Exits with code 1 if any file fails

    The script is NOT an ESLint rule (too complex for the existing setup). It's a standalone check that runs in CI via `node scripts/check-server-actions.ts`.

    **3. Verify dependency-cruiser inbox exception** — read `.dependency-cruiser.cjs` and confirm the `no-cross-feature` rule has the inbox exception: `pathNot: '^src/features/(matcher|classifier)/service\\.ts$'`. If it doesn't exist, add it. This allows `inbox/` to import from `matcher/service.ts` and `classifier/service.ts` for the pipeline orchestrator.

    **4. Pre-commit gate verification** — run the full gate:
    ```bash
    pnpm lint && pnpm typecheck && pnpm test:run && pnpm build && pnpm depcheck
    ```
    Fix any failures. This is the FND-04 requirement.
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm test:run && pnpm build && pnpm depcheck && node scripts/check-server-actions.ts</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/check-server-actions.ts` exists and can be run with `node scripts/check-server-actions.ts`
    - Script finds all `actions.ts` files under `src/features/` (grep: `readdirSync` or `glob`)
    - Script checks for `Result` or `ActionState` return types (grep: `Result|ActionState`)
    - Script exits 0 when all checks pass
    - `pnpm test:run` passes with all 6 FND-03 categories covered:
      - (a) `rls-escape.test.ts` has tests for tenant-scoped models
      - (b) `classifier-fixtures.test.ts` has >= 5 tests (one per label)
      - (c) `matcher-service.test.ts` has tests for all 4 tiebreak paths
      - (d) `act-stage.test.ts` has tests for gates + undo idempotency
      - (e) `budget.test.ts` tests 51st-call blocking
      - (f) `env.test.ts` tests each required field
    - `.dependency-cruiser.cjs` has inbox exception in `no-cross-feature` rule
    - Full pre-commit gate passes: `pnpm lint && pnpm typecheck && pnpm test:run && pnpm build && pnpm depcheck`
  </acceptance_criteria>
  <done>FND-03 all 6 test categories verified with passing tests; FND-04 structural check script created and passing; pre-commit gate green; dependency-cruiser inbox exception confirmed</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Lean acceptance criteria walkthrough</name>
  <files>docs/milestones/lean.md</files>
  <read_first>docs/milestones/lean.md</read_first>
  <action>
    Run the full pre-commit gate first: `pnpm lint && pnpm typecheck && pnpm test:run && pnpm build && pnpm depcheck`. Then start the dev server with `pnpm dev` and walk through each of the 12 acceptance criteria below. For each criterion, note "PASS" or "FAIL" with details. Update `docs/milestones/lean.md` acceptance criterion #7 to replace ">=30 tests" with "category-based coverage per FND-03" after verification.

    **The 12 acceptance criteria:**

    1. Navigate to /applications/new, fill company ("Google"), role title ("Senior PM"), source (linkedin), applied date. Submit. Must complete in <30 seconds.
    2. Go to /settings, click "Sync now". Verify emails appear in /inbox.
    3. Check /applications/[id] for a rejection email match: status = "rejected", auto_status_changed Event exists, undo button visible.
    4. Check /inbox for recruiter outreach: classification = "recruiter_outreach", NO auto-applied status change.
    5. Go to /applications. Verify count badges per status match DB.
    6. Open /applications/[id] with multiple events/stages/emails. Verify chronological order.
    7. Run `pnpm test:run` — all tests pass (category coverage, not count target).
    8. Run `pnpm build` — no errors.
    9. Run `pnpm depcheck` — no violations.
    10. Run `pnpm depcheck` (no-direct-prisma rule) — passes.
    11. Run `node scripts/check-server-actions.ts` — exits 0.
    12. Verify >= 3 real applications exist and >= 1 auto-classification triggered correctly.
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm test:run && pnpm build && pnpm depcheck && node scripts/check-server-actions.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 12 acceptance criteria noted as PASS
    - docs/milestones/lean.md acceptance criterion #7 updated to reference category coverage
    - Dev server starts without errors
    - /inbox page loads and shows needs_review emails
    - /applications page loads and shows applications with status badges
  </acceptance_criteria>
  <done>All 12 Lean acceptance criteria verified and passing; milestone doc updated</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI script -> source files | check-server-actions.ts reads source files to verify structure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-05-07 | Tampering | check-server-actions.ts | accept | Script is a CI check, not a runtime security boundary. False negatives caught by code review. |
| T-05-08 | Elevation of Privilege | RLS escape tests | mitigate | Tests verify that foreign user_id queries return zero rows — the core multi-tenant safety property |
</threat_model>

<verification>
- `pnpm test:run` — all tests pass across all 6 FND-03 categories
- `pnpm lint && pnpm typecheck && pnpm test:run && pnpm build && pnpm depcheck` — full pre-commit gate green
- `node scripts/check-server-actions.ts` — exits 0
- All 12 Lean acceptance criteria verified via manual walkthrough
</verification>

<success_criteria>
- FND-03: all 6 test categories verified with passing tests
- FND-04: pre-commit gate green + structural CI checks pass
- All 12 Lean acceptance criteria verified by human walkthrough
- docs/milestones/lean.md updated to reflect category coverage replacing count target
</success_criteria>

<output>
After completion, create `.planning/phases/05-review-queue-acceptance/05-03-SUMMARY.md`
</output>
