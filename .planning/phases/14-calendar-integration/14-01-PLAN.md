# Plan 04-01: Google Calendar OAuth scope extension + flow

**Phase**: Full-4 (Google Calendar Integration)  
**Goal**: Add calendar scopes to existing OAuth, handle re-auth flow  
**Depends on**: Full-3 complete  
**Effort**: ~1 hour

---

## Tasks

### Task 1: Update OAuth scopes

```typescript
// In auth config
const scopes = [
  // Existing
  "https://www.googleapis.com/auth/gmail.readonly",
  // New
  "https://www.googleapis.com/auth/calendar",
  "https://www.googleapis.com/auth/calendar.events",
];
```

### Task 2: Add re-auth flow to settings

```typescript
// src/app/settings/page.tsx
{!hasCalendarScope && (
  <button onClick={reAuthForCalendar} className="btn btn-primary">
    Connect Google Calendar
  </button>
)}
```

