# Plan 06-02: Dark mode - theme system + CSS variables + toggle

**Phase**: Full-6 (Reminders + Polish)  
**Goal**: Implement dark mode with theme toggle  
**Depends on**: 06-01  
**Effort**: ~1 hour

---

## Tasks

### Task 1: Create theme provider

```typescript
// src/features/theme/theme-context.tsx
export function ThemeProvider({ children }: { children: React.ReactNode }) {
  const [isDark, setIsDark] = useState(() => localStorage.getItem("theme") === "dark");

  useEffect(() => {
    document.documentElement.classList.toggle("dark", isDark);
    localStorage.setItem("theme", isDark ? "dark" : "light");
  }, [isDark]);

  return (
    <ThemeContext.Provider value={{ isDark, toggleTheme: () => setIsDark(!isDark) }}>
      {children}
    </ThemeContext.Provider>
  );
}
```

### Task 2: Update Tailwind for dark mode

```typescript
// tailwind.config.js
module.exports = {
  darkMode: "class",
  // ...
};
```

### Task 3: Add theme toggle to settings

```typescript
// src/app/settings/page.tsx
const { isDark, toggleTheme } = useTheme();
<button onClick={toggleTheme}>
  {isDark ? "☀️ Light mode" : "🌙 Dark mode"}
</button>
```

