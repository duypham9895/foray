# Plan 05-02: Test specs - capture, dashboard, search, tag, undo

**Phase**: Standard-5 (E2E Tests + Acceptance)  
**Goal**: Write 10+ E2E test specs covering key Standard workflows  
**Depends on**: 05-01  
**Effort**: ~1.5 hours

---

## Tasks

### Task 1: Bookmarklet capture flow test

```typescript
// tests/e2e/capture.spec.ts
test("captures job from external page", async ({ page }) => {
  // Navigate to test page with job info
  await page.goto("data:text/html,<h1>Senior Engineer</h1>");
  
  // Execute bookmarklet
  await page.evaluate(() => {
    // Simulate bookmarklet execution
  });

  // Expect redirect to /applications/new
  await expect(page).toHaveURL(/\/applications\/new/);
  
  // Expect form prefilled
  await expect(page.locator("input[name=roleTitle]")).toHaveValue("Senior Engineer");
});
```

### Task 2: Today dashboard load test

```typescript
test("today dashboard loads <500ms", async ({ page }) => {
  const start = Date.now();
  await page.goto("/");
  const elapsed = Date.now() - start;
  expect(elapsed).toBeLessThan(500);
});
```

### Task 3: Search and filter tests

```typescript
test("search finds applications", async ({ page }) => {
  await page.goto("/");
  await page.fill("input[type=text]", "engineer");
  await page.press("input[type=text]", "Enter");
  await expect(page).toHaveURL(/\/search/);
});

test("tag filter works", async ({ page }) => {
  await page.goto("/applications?tag=remote");
  // Verify all visible apps have remote tag
});
```

### Task 4: Undo toast test

```typescript
test("undo toast appears after auto-classify", async ({ page }) => {
  // Trigger auto-classify
  // Expect undo toast
  // Click undo
  // Verify status reverted
});
```

