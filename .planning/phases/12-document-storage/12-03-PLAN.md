# Plan 02-03: Download endpoint + auth + streaming

**Phase**: Full-2 (Document Storage)  
**Goal**: Secure download endpoint with streaming for large files  
**Depends on**: 02-02  
**Effort**: ~1 hour

---

## Tasks

### Task 1: Create download route

```typescript
// src/app/api/documents/[id]/download/route.ts
export async function GET(req: NextRequest, { params }: { params: { id: string } }) {
  const userId = await getCurrentUserId();
  const doc = await db.document.findUnique({ where: { id: params.id } });
  
  if (!doc || doc.uploadedBy !== userId) {
    return NextResponse.json({ error: "Unauthorized" }, { status: 403 });
  }

  const filepath = `data/uploads/${userId}/${doc.applicationId}/${doc.filename}`;
  const stream = fs.createReadStream(filepath);
  
  return new NextResponse(stream, {
    headers: {
      "Content-Type": doc.mimeType,
      "Content-Disposition": `attachment; filename="${doc.filename}"`,
    },
  });
}
```

