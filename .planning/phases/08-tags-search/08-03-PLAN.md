# Plan 03-03: Keyboard shortcut `/` to focus search, integration test coverage

**Phase**: Standard-3 (Tags + Global Search)  
**Goal**: Add `/` keyboard shortcut to focus search bar, add integration tests  
**Depends on**: 03-02  
**Effort**: ~1 hour

---

## Tasks

### Task 1: Add keyboard shortcut listener

```typescript
// src/features/search/hooks/useSearchShortcut.ts
export function useSearchShortcut(searchInputRef: React.RefObject<HTMLInputElement>) {
  useEffect(() => {
    function handleKeyDown(e: KeyboardEvent) {
      // Only when not in text input
      if (e.key === "/" && !isInTextInput(e.target as HTMLElement)) {
        e.preventDefault();
        searchInputRef.current?.focus();
      }
    }

    window.addEventListener("keydown", handleKeyDown);
    return () => window.removeEventListener("keydown", handleKeyDown);
  }, [searchInputRef]);
}

function isInTextInput(el: HTMLElement): boolean {
  return ["INPUT", "TEXTAREA"].includes(el.tagName);
}
```

### Task 2: Integrate into search bar component

```typescript
const ref = useRef<HTMLInputElement>(null);
useSearchShortcut(ref);

return (
  <input ref={ref} type="text" placeholder="Search... (press / to focus)" />
);
```

### Task 3: Integration tests

```typescript
describe("Search integration", () => {
  it("focuses search on / keypress", async () => {
    render(<SearchBar />);
    window.dispatchEvent(new KeyboardEvent("keydown", { key: "/" }));
    expect(document.activeElement?.tagName).toBe("INPUT");
  });
});
```

